databaseChangeLog:
  - changeSet:
      id: 1
      author: postgres
      validCheckSum: 9:2d31e134a0d272cdb83e712eee1d3841
      changes:
        - sql:
            sql: |
              CREATE TABLE lb.service_connection (
                  id INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY NOT NULL,
                  address VARCHAR(255) NOT NULL UNIQUE,
                  type_id INT NOT NULL,
                  FOREIGN KEY (type_id) REFERENCES lb.type_service (id) ON DELETE CASCADE
              );
